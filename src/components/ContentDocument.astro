---
type Props = {
  note?: boolean;
  class?: string;
};

const { note = false, class: className = "" } = Astro.props as Props;
const classes = [note ? "content-note" : "content-document", className]
  .filter(Boolean)
  .join(" ");
---

<div class={classes}>
  <slot />
</div>

---
type Props = {
  class?: string;
};

const { class: className = "" } = Astro.props as Props;
---

<div
  class={`mx-auto w-full max-w-5xl px-3 sm:px-5 lg:px-6 ${className}`.trim()}
>
  <slot />
</div>

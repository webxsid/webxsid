---
type Props = {
  note?: boolean;
  class?: string;
};

const { note = false, class: className = "" } = Astro.props as Props;
const classes = ["content-note", className].filter(Boolean).join(" ");
---

<div class={classes}>
  <slot />
</div>

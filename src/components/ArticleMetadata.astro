---
type Props = {
  date: string;
  minutes?: number;
};

const { date, minutes } = Astro.props as Props;
---

<p class="font-mono text-[11px] uppercase tracking-[0.24em] text-text-muted">
  {date}
  {minutes ? ` · ${minutes} min read` : ""}
</p>
